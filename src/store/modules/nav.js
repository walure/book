// initial state
const state = {
  title: 'movies',
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {
  setNav(state, data) {
    Object.assign(state, data)
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
